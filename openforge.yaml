# OpenForge configuration — auto-generated
# Edit as needed 

providers:
  chain: [openvino, ollama, llamacpp, vllm, lmstudio]

  workloads:
    chat: auto
    embed: auto
    rerank: auto
    code: auto

  openvino:
    enabled: true

  ollama:
    enabled: true
    endpoint: http://localhost:11434

server:
  host: 127.0.0.1
  port: 9090
  timeout: 30

logging:
  level: info
  format: text
